package observer;

/**
 * Represents interface of observer.Observer - class that should react to happened event.
 */
public interface Observer {

    /**
     * Uses then observer.Observer instance must react to event.
     */
    void update();

}
