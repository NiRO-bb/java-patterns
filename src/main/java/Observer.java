/**
 * Represents interface of Observer - class that should react to happened event.
 */
public interface Observer {

    /**
     * Uses then Observer instance must react to event.
     */
    void update();

}
